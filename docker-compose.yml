services:
  rtmp:
    image: tiangolo/nginx-rtmp
    ports:
      - "1935:1935"   # RTMP ingest (glasses push here)
      - "8080:8080"   # HLS playback (backend reads from here)
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    restart: unless-stopped
